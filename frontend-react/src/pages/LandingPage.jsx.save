import { motion } from "framer-motion";
import { Link } from "react-router-dom";

export default function LandingPage() {
  return (
    <div className="h-screen w-full bg-black text-yellow-400 flex flex-col items-center justify-center relative overflow-hidden px-4">

      {/* Glow */}
      <div className="absolute w-[600px] h-[600px] bg-yellow-500/10 blur-[140px] rounded-full" />

      {/* Headline */}
      <motion.h1
        className="text-4xl md:text-6xl font-extrabold text-center z-10 leading-tight"
        initial={{ opacity: 0, y: -40 }}
        animate={{ opacity: 1, y: 0 }}
      >
        سِراج
        <br />
        <span className="text-white">نور يهديك حين تتيه</span>
      </motion.h1>

      {/* Subtitle */}
      <motion.p
        className="text-gray-300 mt-6 text-center max-w-2xl z-10 leading-loose"
        initial={{ opacity: 0 }}
        animate={{ opacity: 1 }}
        transition={{ delay: 0.3 }}
      >
        ليس أداة.
بل رفيق يذكّرك حين تغفل،
ويُعيدك حين تبتعد،
ويضعك أمام نفسك… بلا تزييف.
      </motion.p>

      {/* Value props */}
      <motion.div
        className="flex flex-col md:flex-row gap-3 mt-8 text-sm text-yellow-200 z-10 text-center"
        initial={{ opacity: 0 }}
        animate={{ opacity: 1 }}
        transition={{ delay: 0.5 }}
      >
        <span>🧭 يوجهك عند التشتت</span>
        <span>🧠 يفهم حالتك النفسية</span>
        <spaك</span>
      </motion.div>

      {/* CTA */}
      <motion.div
        className="flex gap-4 mt-10 z-10"
        initial={{ opacity: 0 }}
        animate={{ opacity: 1 }}
        transition={{ delay: 0.7 }}
      >
        <Link to="/login">
          <button className="px-6 py-3 bg-yellow-400 text-black rounded-xl font-bold hover:scale-105 transition">
            ادخل إلى سِراج
          </button>
        </Link>

        <Link to="/register">
          <button className="px-6 py-3 border border-yellow-400 text-yellow-400 rounded-xl font-bold hover:bg-yellow-400 hover:text-black transition">
            ابدأ الآن
          </button>
        </Link>
      </motion.div>

      {/* Footer */}
      <p className="absolute bottom-6 text-xs text-gray-500">
        SIRAJ · guidance system v1
      </p>
    </div>
  );
}
